<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Envoyer une notification multiple</title>
</head>
<body>
    <h2>Envoyer une notification à plusieurs utilisateurs</h2>

    <form method="POST">
        <label>Message :</label><br>
        <textarea name="message" required></textarea><br><br>

        <label>URL (optionnel) :</label><br>
        <input type="text" name="url"><br><br>

        <label>Destinataires :</label><br>
        <select name="recipients" multiple size="5" required>
            {% for user in users %}
                <option value="{{ user.id }}">{{ user.Prenom }} {{ user.Nom }}</option>
            {% endfor %}
        </select><br><br>

        <button type="submit">Envoyer</button>
    </form>

    <br><a href="{{ url_for('notif.show_index') }}">← Retour</a>
</body>
</html>
